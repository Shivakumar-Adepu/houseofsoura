[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm run build"
  publish = "artifacts/house-of-soura/dist/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
